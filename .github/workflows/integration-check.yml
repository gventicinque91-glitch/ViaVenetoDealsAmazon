name: Live integration check

on:
  push:
    branches: [main]
    paths:
      - 'app.py'
      - 'app_v2.py'
      - 'app_v3.py'
      - 'app_v4.py'
      - 'app_v5.py'
      - 'app_v6.py'
      - 'app_v7.py'
      - 'app_v8.py'
      - 'app_v9.py'
      - 'app_v10.py'
      - 'integration_check.py'
      - 'verified_aliases.json'
      - '.github/workflows/integration-check.yml'
  workflow_dispatch:

jobs:
  live-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
      TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
      TELEGRAM_USER_SESSION: ${{ secrets.TELEGRAM_USER_SESSION }}
      TELEGRAM_DEALS_BOT_TOKEN: ${{ secrets.TELEGRAM_DEALS_BOT_TOKEN }}
      VIA_VENETO_PIN: ${{ secrets.VIA_VENETO_PIN }}
      KEEPA_API_KEY: ${{ secrets.KEEPA_API_KEY }}
      SOURCE_CHAT_TITLES: 'Caccia allo SCONTO 🎯|SCONTALO'
      STATE_DIR: './data-ci'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Compile
        run: python -m py_compile app.py app_v2.py app_v3.py app_v4.py app_v5.py app_v6.py app_v7.py app_v8.py app_v9.py app_v10.py integration_check.py generate_session.py
      - name: Run live integration test
        run: python integration_check.py
